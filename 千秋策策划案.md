## 一、命名与定位

### 1.1 项目命名

| 项 | 内容 |
|---|---|
| **中文名** | 千秋策 |
| **英文名** | Millennia Stratagem |
| **中文副标题** | 跨朝代战争卡牌游戏 |
| **英文副标题** | The Dynastic Card Game |

**命名释义：**
- "千秋"强调时间跨度（上古→清，跨两千年），直接传达"不同朝代不同卡包"的核心卖点；
- "策"强调游戏策略性，通过不同的牌组构筑和下牌时机，在随机抽牌下达到击败对方的目标；
- 英文 `Dynastic`强调不同的朝代

---

### 1.2 项目定位

**一句话定位：**
一款以中国古代历朝兵种与战争器械为主题的 1v1 回合制卡牌游戏，玩法对标 KARDS。

**对标产品：**
- **核心对标**：KARDS（回合节奏、指挥点费用、ATK+HP 卡面）
- **辅助参考**：Into the Breach（站位与威胁范围预判）、炉石传说（卡牌效果表述习惯）

**核心差异化：**
| 维度 | KARDS | 千秋策 |
|------|-------|---------------|
| 题材 | 二战各国阵营 | 中国古代历朝 |
| 战场 | 无棋盘，前线机制 | 35453 五排容量制战场，无固定格位、排内相邻成链（中军部署、前军交战） |
| 单位行动 | 攻击/移动独立 | 通用机制加单位独有机制 |
| 卡包逻辑 | 国家阵营 | 朝代卡包（可混朝组卡） |

---

### 1.3 开发目标（Demo 边界）

**目标：** 制作可运行的单机 Demo（人机对战或热座对战）

**Demo 阶段明确不做：**
- 网络对战（PvP 联机）
- 付费/抽卡系统
- 复杂 AI（先写基于规则的简单 Bot）
- 音效与复杂特效
- 多语言本地化

**Demo 阶段必须做到：**
- [ ] 2 个朝代有足够的朝代卡牌构筑一套（30）张卡牌用于对战
- [ ] 完整的回合流程（补给→抽牌→行动→结束）
- [ ] 战场部署、移动、攻击、胜负判定
- [ ] 可拖拽的 uGUI 手牌与战场交互
- [ ] 代码结构清晰（Manager + 数据驱动）

---

### 1.4 技术栈

| 层级 | 技术 |
|------|------|
| 引擎 | Unity 2022.3 LTS |
| 语言 | C# |
| UI | uGUI（Canvas + Image + 拖拽事件） |
| 数据 | ScriptableObject（卡牌配置） |
| 动画 | DOTween（免费版）或原生 Animator |
| 版本控制 | Git + GitHub / Gitee |

---

### 1.5 受众与平台

- **目标平台**：

| 平台 | 分辨率 |
|------|------|
| PC（Windows / Mac）|分辨率 1920×1080 适配|
| Mobile | 竖屏自适应分辨率 |

- **核心受众**：策略卡牌玩家、历史题材爱好者

## 二、核心玩法

### 2.1 核心循环

每回合获得指挥点 → 抽牌 → 打出手牌召唤单位/释放策略（耗部署费用 CP）/ 控制单位行动（每次行动耗 1 AP ＋ 行动费用 CP，见 §2.5）→ 消灭对方大营获胜。

### 2.2 胜利条件

| 条件 | 说明 |
|------|------|
| **主要胜利** | **敌方大营 HP 归零** |
| **牌库耗尽（新增）** | 牌堆摸空后每次抽牌失败，抽不到牌的一方**自己的大营**扣血（2→4→6…，见 §5.4.1）。被疲劳打空大营与被打死等价，这是第二条通往胜利的路径 |

### 2.3 战场：35453 五排容量制（无固定格位）

对战空间（数字为该排容量上限；建筑为链中锚点）
```
后军(容量3):  [军械库] ~单位~ [粮草]          ┐
中军(容量5):   ~单位~ [大营] ~单位~           │ 敌方
前军(容量4):    ……纯单位链，无建筑、无格位…… │ 共享
中军(容量5):   ~单位~ [大营] ~单位~           │ 己方
后军(容量3):  [粮草] ~单位~ [军械库]          ┘
```

- **无格位、无固定位置**：场上不存在固定编号的格位或坐标。**每排 = 一个容量容器 + 一条"相邻链"**：该排内的单位与建筑按入场的先后顺序连成一条线性链，每个成员至多有左、右 **2 个邻居**。链内先后**不影响移动、攻击与容量规则**，仅「守护」判定**同排相邻**时用到（见 §4.3）。
- **容量**：后军 3（军械库 + 粮草 占 2，剩 **1 个单位容量**）、中军 5（大营 占 1，剩 **4 个单位容量**）、共享前军 4（无建筑，全为 4 个单位容量）。容量是该排唯一的硬限制。
- **锚点建筑**：大营固定存在于己方中军、军械库与粮草固定存在于己方后军，作为所在排链条中的**建筑锚点成员**，占容量、不可移动、不可被替换；入排的单位绕它们左右贴放。无"建筑固定位于排中央"一说——大营在链中的位置完全由玩家如何在其左右贴单位决定。
- **入排摆放规则（部署 / 袭扰骑兵进入等）**：要把一个新单位放进某排时，玩家从该排**现有成员中任选一个**，把新单位放到它的**左边或右边**（插到它之前/之后，或插到相邻两成员之间）；该排容量未满即可，**无其他任何限制**。例：中军可把 4 个单位全部贴到大营的同一侧，只要该排不超容量 5。前军初始为空，第一个入场的单位成为该链的唯一成员，此后仍按同一规则贴放。
- 单位状态 = `所在排` + 该成员在排内链上的**相邻关系**；程序侧以 `enum RowType { Back, Mid, Front }` + 所属方 + **双向链表节点**（`prev` / `next` 引用）表示，不使用格子坐标或整数格位。链表节点**仅用于「守护」的相邻判定**，不参与移动/攻击/容量计算。
- **排顺序（决定移动与攻击方向）**：己方后军 → 己方中军 → 共享前军 → 敌方中军 → 敌方后军。**排距 = 两排序号之差**，移动只能进入**相邻排（排距 1）**。
- **移动方向**：单位只能**向前移动**（与攻击同向）；**唯一例外是袭扰骑兵可以撤回**（敌方中军 → 共享前军，详见 §7.4）。移动与攻击均为**跨排**行为，与排内链的先后无关。
- **攻击方向**：固定向前，射程按兵种自带（详见 §7.1.1）。
- **部署规则**：单位默认部署进己方中军（**单位容量**未满时）；当中军单位容量已满、且其中驻有敌方袭扰骑兵时，可改部署进己方后军（详见 §8.1.1）。
- 大营固定存在于己方中军，占 1 容量但**不占那 4 个单位容量**，不可移动、不可部署。
- 后军容量 3 = 军械库 + 粮草 + 至多 1 个单位；该单位容量被占用后，后军无法再入单位。

建筑存活时无额外效果；**摧毁敌方建筑后向敌方施加Debuff**。

| 建筑 | HP | 被摧毁后（Debuff） |
|------|-----|-------------------|
| **大营** | 20 | HP 归零 → **立即判负**（唯一胜负条件） |
| **军械库** | 5 | 敌方全场兵牌和后续上场兵牌 **ATK -1**（军械尽失，武备废弛） |
| **粮草** | 5 | 敌方 **CP 上限 -2**（粮草被焚，补给断绝） |

> 大营位于中军、军械库与粮草位于后军：攻击后军建筑需要弓兵/器械（自带射程 2）从共享前军攻击，或由骑兵袭扰（§7.4）进入敌方中军后攻击。

通用卡牌中配置抢修/维修类策略卡，消耗cp对军械库粮草库进行修缮，修缮完后无敌一回合，防止反复刷建筑

> **设计意图**：建筑作为"可选择的战术目标"，不摧毁不影响对局，但摧毁后可逐步瓦解敌方战力。鼓励进攻型打法，避免双方龟缩前军对峙。

### 2.4 回合流程（阶段状态机）
```
游戏开始
|—— 抽5张牌，可以选一张放回重新抽取
|—— 抛硬币区分先后手
回合开始
├─ 1. 补给阶段：CP 上限 +1 并补满
├─ 2. 抽牌阶段：抽 1 张（牌堆已空则本次抽牌失败，改为结算「牌库耗尽」疲劳伤害，见 §5.4.1）
├─ 3. 主阶段（自由操作，可反复）：
│       - 打出单位卡 → 放进己方中军链（单位容量未满时；单位容量已满且驻有敌方袭扰骑兵时，改放后军链）
│       - 打出策略卡 → 结算效果
│       - 移动单位（移动到相邻排，消耗 1 AP ＋ 行动费用 CP，见 §2.5）
│       - 攻击（消耗 1 AP ＋ 行动费用 CP）
│       - 骑兵移动进入敌方中军 → 进入袭扰状态（§7.4）
│       - 袭扰骑兵撤回己方前军（消耗 1 行动点，清除袭扰与 ATK 加成）
├─ 4. 结束阶段：清理"本回合"标记
└─ 切换玩家
```

回合结束判定
```
| 方式 | 触发条件 | 说明 |
|------|----------|------|
| 手动结束 | 玩家点击【结束回合】按钮 | 主阶段任意时刻，玩家确认操作完毕后点击 |
| 超时托管 | 倒计时60秒归零 | 从玩家最后一次操作后开始计时（或回合开始时），超时后AI自动执行一个最优操作并结束回合 |
```

### 2.5 单位经济：部署费用 / 行动费用 / 行动次数（三字段口径）

> 每张卡把资源消耗拆成 **部署费用、行动费用、行动次数** 三个字段，并在卡池表里各占一列列出；与数据表《基础公式》C7:D8（dcs / acs）、《部署价值表》E3 及《赳赳老秦》卡池的「费用/行动力」列口径对应。

| 字段 | 卡面位置 | 含义 | 谁填 |
|------|---------|------|------|
| **部署费用**（dcs）| 左上 cost | 打出该卡**一次性**支付的命令点（CP），= 现有"费用" | 每张卡 |
| **行动次数** | 兵种固有 | 每回合最多"行动"几次，由固定 AP 上限决定（见下） | 兵牌（策略牌无） |
| **行动费用**（acs）| 兵种固有 | 每执行 **1 次移动或攻击** 额外消耗的 CP（从 CP 池扣），**与行动次数互不影响** | 兵牌（策略牌无） |

**行动消耗规则（行动 = 耗 AP ＋ 耗 CP）：**
- 每个单位有**固定 AP 上限 = 行动次数**：**非骑兵 AP=1**（每回合只能行动一次，攻击或移动二选一）；**骑兵 AP=2**（须"移动后攻击/攻击后移动"，不支持"移动+移动"）；**连战**单位 AP=2（可"攻击+攻击"，每次单独结算，也可只打一次）。
- **每次行动消耗 1 AP ＋ 该单位的「行动费用」CP**。行动 CP 从玩家本回合的指挥点池扣除——**出牌与行动共用同一 CP 池**；AP 在每回合补给阶段重置为行动次数。
- **行动费用不计入词条、不占词条等价费用**，按兵种/重量/机动性取值（与 §4.3 兵种固有射程、骑兵双 AP 同口径），参考档位见下表。
- **行动费用 × 行动次数 ≠ 相乘**：行动次数只由 AP 决定，行动费用只决定每次操作的扣费；两者互不影响。

| 兵种（参考档位，可按卡微调） | 行动次数(AP) | 行动费用(CP/次) |
|---|---|---|
| 步兵 · 轻 / 中 | 1 | 1 / 1 |
| 步兵 · 重 | 1 | 2 |
| 弓兵 · 轻 / 重 | 1 | 1 / 2 |
| 器械（支援） | 1 | 2 |
| 骑兵 · 轻 / 中 | 2 | 1 / 2 |
| 连战单位 | 2 | 依兵种 |
| 重型精锐单位 | 1 | 2 |
| 策略牌 | —（无行动） | —（无行动） |

**衍生规则：**
- 移动 = 进入相邻排（排距 1）；攻击 = 指定射程内目标。两者均消耗 1 AP ＋ 行动费用 CP。
- **袭扰撤回**：袭扰骑兵可从敌方中军撤回己方前军，消耗 1 AP ＋ 行动费用，是唯一可向后移动的单位（§7.4）。
- **新部署单位**当回合不能行动（行动次数当回合按 0），除非有「闪击」。
- 行动顺序回合内任意，由玩家自主决策。

### 2.6 单位属性

| 属性 | 说明 |
|------|------|
| **color**（卡面）| 卡牌稀有度 |
| **cost**（左上）| 部署消耗的 CP 值 |
| **name**（中上）| 卡牌名称|
| **keyword**（右上/名称下方）| 卡牌携带的特殊词条，无则留空|
| **des**（中）| 卡牌描述，包括风味文本和卡牌效果|
| **unitType**（兵种）| 步兵/骑兵/弓兵/器械（支援），决定 β/α 系数与自带射程|
| **ATK**（左下） | 攻击力，攻击时按此值扣减目标 HP |
| **HP**（右下） | 生命值，≤0 时单位死亡 |

**射程（兵种自带，不计入词条、不占词条等价费用）：**

| 兵种 | 射程 | 说明 |
|------|------|------|
| 步兵 / 骑兵 | 1 排 | 只能攻击正前方相邻排 |
| 弓兵 / 器械（支援） | 2 排 | 可越过共享前军攻击敌方中军（**含大营**），无需先进前军 |
| **仅器械（支援）** | 2 排 | 行动次数固定为 **1**（每回合只能攻击一次）；行动费用固定 **2**（不分轻重） |

**反击规则（同类互攻才反击）：**
- 近战 ↔ 近战（步兵/骑兵）：互相攻击，双方均触发反击；
- 远程 ↔ 远程（弓兵/器械）：互相攻击，造成反击伤害；
- 近战 ↔ 远程交叉攻击：被攻击方**不反击**（远程攻击时对方不反击，远程被攻击时也不反击）。

### 2.7 胜负条件
- **大营 HP 归零 → 立即判负**
- 大营视为"建筑"单位，有独立 HP（Demo 设为 20，与 §2.3 建筑表一致）
- 大营固定存在于己方中军，作为该排相邻链的建筑锚点成员，占 1 容量但不占那 4 个单位容量，不可移动、不可被部署替换

---

## 三、技术架构设计

### 3.1 技术栈
| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 引擎 | Unity 2022.3 LTS | 稳定，LTS 支持到 2025 |
| UI | uGUI + TextMeshPro | 做 2D Demo 直接复用 Canvas 经验 |
| 动画 | DOTween (免费版) | UI 位移/缩放/淡入淡出，零代码动画 |
| 数据 | ScriptableObject | 卡牌配置、朝代配置、AI 策略配置全部 SO 化 |
| 架构模式 | Manager + 事件驱动 | 单例管理器负责流程，事件系统解耦 UI 与逻辑 |
| 版本控制 | Git + GitHub | 简历必须附仓库链接 |

### 3.2 目录结构
```
Assets/
├─ _Project/
│  ├─ Scripts/
│  │  ├─ Core/              ← 纯逻辑层（不引用 UnityEngine.UI）
│  │  │  ├─ Data/           ← ScriptableObject 定义
│  │  │  ├─ Managers/       ← 单例管理器
│  │  │  ├─ Models/         ← 运行时数据类（Unit, PlayerState）
│  │  │  ├─ Battle/         ← 战斗计算、排位与容量、射程判定
│  │  │  └─ Utils/          ← 工具类、事件系统、对象池
│  │  │
│  │  └─ UI/                ← 表现层（引用 uGUI）
│  │     ├─ Cards/          ← 卡牌 UI、拖拽、手牌区
│  │     ├─ Board/          ← 战场排位容器、单位渲染
│  │     ├─ HUD/            ← CP 条、回合提示、胜负弹窗
│  │     └─ Animations/     ← DOTween 动画封装
│  │
│  ├─ ScriptableObjects/
│  │  ├─ Cards/             ← 单位卡、策略卡 .asset
│  │  ├─ Factions/          ← 朝代配置（秦、汉）
│  │  └─ Effects/           ← 策略卡效果预设
│  │
│  ├─ Prefabs/
│  │  ├─ UI/                ← 卡牌预制体、排容器预制体
│  │  └─ VFX/               ← 攻击特效、受击特效
│  │
│  └─ Scenes/
│     ├─ Boot.unity         ← 初始化场景
│     ├─ Menu.unity         ← 主菜单
│     └─ Battle.unity       ← 对战场景
```
### 3.3 事件系统设计
#### 3.3.1 概述
事件系统用于解耦逻辑层（Managers）和表现层（UI），实现"逻辑触发事件，UI自动响应"的架构。
所有游戏状态变化（出牌、死亡、回合切换等）都通过事件广播，UI组件监听对应事件后更新自身。

#### 3.3.2 事件枚举定义
```C#
public enum GameEventType
{
    // === 回合流程 ===
    TurnStarted,           // 回合开始
    PhaseChanged,          // 阶段切换（补给→抽牌→主阶段→结束）
    TurnEnded,             // 回合结束

    // === 卡牌 ===
    CardDrawn,             // 抽牌
    CardPlayed,            // 打出卡牌
    CardDiscarded,         // 弃牌（手牌被弃置，直接退场）
    CardRemoved,           // 卡牌退场（结算完的策略卡/死亡单位/超上限手牌，本局不再回到牌堆）
    HandFull,              // 手牌已满

    // === 单位 ===
    UnitDeployed,          // 单位部署
    UnitMoved,             // 单位移动（跨排）
    UnitAttacked,          // 单位攻击
    UnitDamaged,           // 单位受伤
    UnitDied,              // 单位死亡
    UnitRaidStarted,       // 骑兵进入敌方中军（袭扰开始）
    UnitRaidTicked,        // 袭扰每回合结算（ATK+1、受到2点伤害）
    UnitRaidEnded,         // 袭扰结束（骑兵撤回己方前军，移除 ATK 加成）

    // === 建筑 ===
    BuildingDamaged,       // 建筑受损
    BuildingDestroyed,     // 建筑被摧毁
    BuildingRepaired,      // 建筑被修缮
    BuildingInvincibleChanged, // 建筑无敌状态变化

    // === 战斗 ===
    DamageDealt,           // 造成伤害
    CounterAttackTriggered,// 触发反击
    Kill,                  // 击杀

    // === 游戏流程 ===
    GameStarted,           // 游戏开始
    MulliganCompleted,     // 换牌完成
    GameOver,              // 游戏结束
    Victory,               // 胜利
    Defeat,                // 失败

    // === 资源 ===
    CPChanged,             // 指挥点变化
    APChanged,             // 行动力变化

    // === 牌库耗尽（疲劳，见 §5.4.1）===
    DeckEmpty,             // 牌堆清空（本次抽牌失败）
    FatigueTriggered,      // 牌库耗尽结算一次（大营扣血，数值逐次 +2 递增）
}
```

#### 3.3.3 事件参数定义
```C#
// 事件参数基类
public abstract class GameEventArgs
{
    public GameEventType EventType { get; protected set; }
}

// 单位死亡事件参数
public class UnitDiedEventArgs : GameEventArgs
{
    public Unit Attacker { get; set; }        // 攻击者
    public Unit Target { get; set; }           // 目标
    public int Damage { get; set; }             // 伤害值
    public bool IsCounterAttack { get; set; }  // 是否反击

    public UnitDiedEventArgs()
    {
        EventType = GameEventType.UnitDied;
    }
}

// 打出卡牌事件参数
public class CardPlayedEventArgs : GameEventArgs
{
    public CardData Card { get; set; }         // 打出的卡牌
    public PlayerController Player { get; set; } // 打出者
    public RowType? TargetRow { get; set; }     // 部署排（单位卡）或目标排（策略卡）
    public int CostPaid { get; set; }           // 消耗的CP

    public CardPlayedEventArgs()
    {
        EventType = GameEventType.CardPlayed;
    }
}

// 回合切换事件参数
public class TurnChangedEventArgs : GameEventArgs
{
    public int TurnNumber { get; set; }         // 当前回合数
    public PlayerController ActivePlayer { get; set; } // 当前行动玩家
    public GamePhase NewPhase { get; set; }     // 进入的新阶段

    public TurnChangedEventArgs()
    {
        EventType = GameEventType.TurnStarted;
    }
}

// 牌库耗尽（疲劳）事件参数 —— §5.4.1
public class FatigueEventArgs : GameEventArgs
{
    public PlayerController Player { get; set; }  // 抽不到牌、要扣自己大营的那一方
    public int Damage { get; set; }               // 本次伤害 = 2 * 累计触发次数（2/4/6/8…）
    public int TriggerCount { get; set; }         // 累计触发次数（该玩家自己的计数器）
    public int CampHpAfter { get; set; }          // 结算后己方大营剩余 HP

    public FatigueEventArgs()
    {
        EventType = GameEventType.FatigueTriggered;
    }
}
```
|事件	|参数类	|需要传递的数据|
|------|-------|-------|
|TurnStarted	|TurnChangedEventArgs	|回合数、当前玩家、阶段|
|PhaseChanged	|TurnChangedEventArgs	|新阶段|
|CardPlayed	|CardPlayedEventArgs	|卡牌、玩家、目标排、消耗CP|
|CardDrawn	|CardDrawnEventArgs	|卡牌、玩家|
|CardRemoved	|CardRemovedEventArgs	|卡牌、玩家、退场原因（结算/弃置/死亡/超上限）|
|UnitDeployed|	UnitDeployedEventArgs|	单位、部署排、玩家|
|UnitMoved	|UnitMovedEventArgs	|单位、起始排、目标排|
|UnitAttacked|	UnitAttackedEventArgs|	攻击者、目标、伤害|
|UnitDied	|UnitDiedEventArgs	|攻击者、目标、伤害、是否反击|
|UnitDamaged|	UnitDamagedEventArgs|	目标、伤害、来源|
|UnitRaidStarted|	UnitRaidEventArgs	|袭扰骑兵、进入排（敌方中军）、敌方玩家|
|UnitRaidTicked|	UnitRaidEventArgs	|袭扰骑兵、当前ATK、当前HP|
|UnitRaidEnded|	UnitRaidEventArgs	|袭扰骑兵、撤回后排、被移除的ATK加成|
|BuildingDestroyed|	BuildingDestroyedEventArgs	|建筑类型、摧毁者|
|BuildingRepaired	|BuildingRepairedEventArgs	|建筑类型、修缮者|
|GameOver	|GameOverEventArgs	|胜利者、失败者、回合数|
|CPChanged	|CPChangedEventArgs	|玩家、旧值、新值、变化量|
|DeckEmpty	|DeckEmptyEventArgs	|玩家、抽牌失败次数|
|FatigueTriggered	|FatigueEventArgs	|玩家、本次伤害值、累计触发次数、大营剩余HP|

#### 3.3.4 事件管理器实现
```C#
public static class EventManager
{
    private static Dictionary<GameEventType, Delegate> eventDictionary = 
        new Dictionary<GameEventType, Delegate>();

    // 注册事件监听
    public static void Subscribe<T>(GameEventType eventType, Action<T> listener) 
        where T : GameEventArgs
    {
        if (eventDictionary.ContainsKey(eventType))
        {
            eventDictionary[eventType] = Delegate.Combine(eventDictionary[eventType], listener);
        }
        else
        {
            eventDictionary[eventType] = listener;
        }
    }

    // 取消注册事件监听
    public static void Unsubscribe<T>(GameEventType eventType, Action<T> listener) 
        where T : GameEventArgs
    {
        if (eventDictionary.ContainsKey(eventType))
        {
            eventDictionary[eventType] = Delegate.Remove(eventDictionary[eventType], listener);
        }
    }

    // 触发事件
    public static void Trigger<T>(T args) 
        where T : GameEventArgs
    {
        if (eventDictionary.TryGetValue(args.EventType, out var delegate))
        {
            (delegate as Action<T>)?.Invoke(args);
        }
        
        // 调试日志
        Debug.Log($"[EventManager] {args.EventType} triggered");
    }
}
```

#### 3.3.5 事件生命周期管理
```C#

- UI组件（如BoardUI、HandUI、HUD）：在OnEnable中注册事件，在OnDisable中取消注册
- 核心逻辑组件（如GameManager、BattleManager）：在Awake中注册，在OnDestroy中取消注册
- 避免重复注册：使用SubscribeOnce扩展方法，确保同一监听器只注册一次

// 示例：UI组件注册事件
public class BattleHUD : MonoBehaviour
{
    private void OnEnable()
    {
        EventManager.Subscribe<CPChangedEventArgs>(GameEventType.CPChanged, OnCPChanged);
        EventManager.Subscribe<UnitDiedEventArgs>(GameEventType.UnitDied, OnUnitDied);
    }

    private void OnDisable()
    {
        EventManager.Unsubscribe<CPChangedEventArgs>(GameEventType.CPChanged, OnCPChanged);
        EventManager.Unsubscribe<UnitDiedEventArgs>(GameEventType.UnitDied, OnUnitDied);
    }
}
```

#### 3.3.6 UI事件响应映射表

| 事件 | 监听者 | 响应行为 |
|------|--------|----------|
| TurnStarted | PhaseIndicator | 更新阶段提示文字为"第X回合 - 补给阶段" |
| TurnStarted | CPBarUI | 更新CP条显示 |
| PhaseChanged | PhaseIndicator | 更新阶段提示文字 |
| CardPlayed | HandUI | 从手牌区域移除该卡牌图标 |
| CardPlayed | BoardUI | 在部署排生成单位图形（单位卡）或播放特效（策略卡） |
| CardPlayed | CPBarUI | 扣减CP显示 |
| UnitDied | BoardUI | 播放死亡动画，从战场移除单位图形 |
| UnitDied | DamageNumberUI | 显示伤害数字 |
| UnitRaidStarted | BoardUI | 袭扰骑兵显示红色标记与倒计时 |
| UnitRaidTicked | UnitUI | 刷新袭扰骑兵 ATK/HP，提示自毁倒计时 |
| UnitRaidEnded | BoardUI | 移除袭扰标记与倒计时，ATK 回退到卡面基础值 |
| BuildingDestroyed | BuildingUI | 播放建筑摧毁动画，更新建筑状态 |
| BuildingRepaired | BuildingUI | 播放修缮动画，显示无敌护盾图标 |
| GameOver | GameOverPanel | 弹出结算面板 |
| CPChanged | CPBarUI | 更新CP条数值和动画 |
| DeckEmpty | DeckUI | 牌堆图标显示为空状态 |
| DeckEmpty | PhaseIndicator | 抽牌阶段飘字"牌库已空" |
| FatigueTriggered | DamageNumberUI | 在大营上方飘红色伤害数字（区别于战斗伤害） |
| FatigueTriggered | HUD | 刷新常驻的"疲劳：每次扣 X 点"提示（见 §5.4.1） |

#### 3.3.7 事件调试与日志
- 所有事件触发时，自动打印日志：`[EventManager] {EventType} triggered`
- 提供EventDebugger组件（可选），可在场景中挂载，实时显示最近触发的事件列表
- 日志级别控制：可通过配置文件开关事件日志（默认开启，方便调试）


## 四、卡牌设计体系
### 4.1卡面布局
```C#
┌─────────────────────┐  <- 边框颜色，区分卡牌稀有度白/蓝/紫/金
│ 2 1   秦锐士    [步] │  ← 部署费用、行动费用 / 名称 / 兵种标识
│                     │
│    [卡牌插画区域]    │
│                     │
│   魏之武卒..."       |  ← 古文描述（斜体，风味文本）
│                     │
│   部署: 无特殊效果   │  ← 效果文本（如有）
│                     │
│     3        4      │  ← ATK / HP
└─────────────────────┘
```

**卡面字段定义（与程序侧 ScriptableObject 一一对应）：**

| 字段 | 位置 | 说明 |
|------|------|------|
| `cost` | 左上 | 部署消耗的 CP |
| `name` | 中上 | 卡牌名称 |
| `typeIcon` | 右上 | 兵种标识（步/骑/弓/策） |
| `keyword` | 名称下方 | 词条标签（闪击/连战/重甲等） |
| `flavorText` | 中部 | 古文风味描述，无实际效果 |
| `effectText` | 中下部 | 实际效果说明（如有） |
| `atk` | 左下 | 攻击力（策略卡留空） |
| `hp` | 右下 | 生命值/血量（策略卡留空） |

---

### 4.2 卡牌类型

| 类型 | 标识 | 特点 |
|------|------|------|
| **兵牌** | 步/骑/弓/器 | 有 ATK/HP，需部署到战场，受 AP 限制；射程由兵种自带（步/骑 1 排，弓/器 2 排）|
| **策略牌** | 策 | 无 ATK/HP，打出后立即结算效果，不进入战场 |

---

### 4.3 词条系统（Keywords）

> 所有词条在卡面以**金色/红色标签**显示，玩家一眼识别。
> 以下词条抄自《数据表demo.xlsx·基础公式》**B26:D64**（词条 | 效果 | 等价费用 | 备注 四列），**一律以数据表实际为准**。
> 表中未登记的条目不得出现在卡池里；带 ★ 的是基准表已登记、但本次两朝卡池未用到的档位（保留备用）。

| 词条 | 效果 | 等价费用 | 备注 |
|------|------|---------|------|
| **闪击** | 部署回合即可移动或攻击 | 0.5 | 骑兵携带时等价为 0；未携带记 -0.5 |
| **伏兵** | 主动攻击前不可被攻击 | 0.5 | |
| **守护** | 单位存活时，**同排相邻链中与其左右紧邻**的友方单位与建筑不能被选中 | 1 | 携带者部署时 **+1 HP**（已含在卡面 HP 内）；**仅同排、仅链上左右紧邻成员，含建筑**（可替大营/军械库/粮草挡刀）；**守护单位之间不互相保护**，带守护的单位始终可被选中 |
| **血战** | 攻击后回复 2 HP | 1.5 | 基础表 B30 定档 1.5（非旧稿的 2.0）；旧版「部署时 +1 ATK」已废除 |
| **连战** | 同一回合可连续攻击两次 | 1 | 两次攻击独立计算；携带者行动次数按 2 计 |
| **重甲** | 每层重甲使受到的伤害 -1 | 重甲1 = 1 / 重甲2 = 2.5 / 重甲3 = 4 | 仅重型单位可携带（基础表 B42:B44 三个独立档位，非线性） |
| **摸牌** | 抽取 1/2/3 张牌 | 1/3/6 | |
| **召唤** | 添加一张卡牌进入**牌堆** / 将牌堆中一张牌加入**手牌** | 牌堆 0.5 / 手牌 1 | 手牌档要求牌堆中确有该牌，没有则不生效 |
| **回血**（加血类）| 为**建筑**回复 3 / 5 / 7 HP；为**兵**回复 3 HP；攻击后为**大营**回复 2 HP | 建筑 1 / 2 / 3；为兵回复3 = 2；大营 1.5 | 建筑档「仅限策略卡」；大营档「仅限兵种卡」，走词条不走效果 |
| ★ **压制** | 限制敌方单位一回合行动 | 1 | 仅策略卡，仅可作用于兵牌（B48） |
| ★ **抵抗** | 免疫压制效果 | 0.5 | 仅兵牌；重甲3 以 0 费附带该词条（B49） |
| ★ **反制** | 埋伏型解场牌（陷阱）：**不能主动打出**，只在触发条件满足时**自动发动**，不消耗费用，起出奇制胜的解场作用 | **未定价** | 基准表 B45 只写了触发说明，**未给触发条件、未给具体效果**，故无法定价。定位已明确，已确认示例：**「对方攻击己方大营的第一个单位，攻击后立即死亡」**（触发条件 = 敌方单位攻击己方大营；触发效果 = 该攻击者攻击后立即死亡）。落表前需补 ①触发条件枚举 ②效果的基准锚点；**暂不进入卡池** |
| **buff 类**（**不是词条**）| 增加 atk / 增加 hp / 同时增加 atk 和 hp | 每点 atk 0.75 / 每点 hp 0.5 / 每点各计 0.6 | 见下方说明；**只写进「效果」栏，不占词条栏** |

> **射程不是词条**：弓兵与器械（支援）**兵种自带射程 2**，攻击时不受反击、被攻击时也不反击；近战类（步兵/骑兵）自带射程 1 排。骑兵 2 AP 同理为兵种自带（§2.5）。

> **支援（器械）类定位 =「部署增益型」**：支援类的价值主要来自**部署时给友方提供的增益**，该增益属于 **buff 类效果**（基础表 B50:B52），**写在卡面「效果」栏、不作为独立词条**（与「誓师」这类旧写法不同）。
> - **计价**：基础表 B50 备注限定「仅限策略卡**和支援卡**的部署效果，增加数值的费用不得超过 3 费」，因此支援卡带 buff 是合规的；支援类 β/α = **1/1**（基础表 E12:F12），面板与预算同量纲。
> - **本次两朝各 1 张**：秦·连弩台、汉·治粟都尉 —— 3 费 · 重型 · 3/4 · 2 行动费用 · **1 次行动** · 射程 2，效果「部署当回合，指定一个友方单位本回合 HP+3」（1.5 费），卡牌价值 8.5 对预算 8.208。
> - **注意**：器械**每回合只能攻击一次**（行动次数 1，与骑兵的 2 次不同）；射程 2 使其仍可从共享前军补刀敌方后军建筑。

> **守护的覆盖范围与边界（基于排内"相邻链"）：**
> - **判定对象**：守护单位保护的是它在本排相邻链中的**直接左/右邻居**（友方单位与建筑）。链中被其他成员隔开的成员不属于"相邻"，不受该守护保护。
> - **示例**：中军链（自左至右）`[卫2·守护][卫1·守护][大营][弓手]`——卫1 是大营的直接左邻、也是卫2 的直接右邻；因"守护互不保护"，卫1 对同为守护单位的卫2 无效，故卫1 实际**只替大营挡刀**；弓手紧邻大营右侧但无守护词条，不保护任何人。后军链 `[军械库][盾·守护][粮草]`：盾同时是军械库与粮草的**直接邻居**，可同时替二者挡刀。
> - 守护单位**自身始终可被选中**；被守护的目标**攻击与策略卡都无法指定**。
> - **守护单位之间不互相保护**：否则中军 `[守护][守护][大营][守护][守护]` 会让整排与大营**都无法被选中**，单体伤害体系被锁死、对局可能永远无法结束。互不保护后，敌方只需先击杀保护者即可解除保护，大营不存在"无敌"状态。
> - **布置会改变守护关系（无格位模型的取舍点）**：向链中**再贴入一个成员**，会切断守护者与被保护目标之间的相邻、保护随即失效；反之把带守护的单位贴到目标旁即可建立保护。因此玩家需主动管理排内链的松紧，敌方也可用能进入该排的棋子（如袭扰骑兵）挤占相邻关系。
> - **天然克星**：以**排**为目标的范围效果不"选中"具体单位，**无视守护**，可直接打击被守护的排。基准表只登记了「对指定一排单位造成 2 点伤害」（B64 = 3 费）这一档；**不存在**"对目标排**及其相邻排**"的写法，如需相邻排版本请先在《基础公式》里补条目并定价。
> - **守护的极限（设计边界）**：同一成员至多有左、右 **2 个直接邻居**，因此**大营最多同时被 2 个守护单位保护**（紧贴其左右各一个）；把更多单位继续往同一侧贴放不会增加对大营的保护——隔了其他成员的守护者已不再与"大营"相邻。与此相对，**中军 4 个单位容量若全部填满**，袭扰骑兵因无空容量**无法进入**敌方中军，这是"堆满中军"的收益；只要中军留有余量，袭扰骑兵即可进入并挤到其余成员的旁侧（§7.4）。

---

### 4.4 卡组设计

> **本节由 `build_dynasty_sheets.py` 在每次构建时自动重写**，直接输出《数据表demo.xlsx》两个卡池的全部 48 张卡，
> 与表格逐格一致。修改卡池请改脚本里的 `QIN` / `HAN` 并重跑构建，**不要手改下面的表**——手改会在下次构建时被覆盖。

> 列说明：**部署费用** = dcs（一次性 CP）；**行动费用** = acs（每次移动/攻击的 CP）；**行动次数** = AP（骑兵 2、连战 2、其余 1）。
> 「词条」栏只允许写《基础公式》B26:D64 登记过的条目；带 buff 的卡把增益写进「效果」栏。

<!-- POOL:START -->
#### 秦朝卡池（Qin，24 张）

| cardId | 名称 | 稀有度 | 类型 | 兵种 | 重量 | 部署费用 | 行动费用 | 行动次数 | 射程 | ATK | HP | 词条 | 效果 | 古文描述 | 出处 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| qin_001 | 什伍卒 | 普通 | 兵牌 | 步兵 | 轻 | 1 | 1 | 1 | 1 | 1 | 2 | — | — | 令民为什伍，而相牧司连坐。 | 《史记·商君列传》 |
| qin_002 | 戍卒 | 普通 | 兵牌 | 步兵 | 中 | 2 | 1 | 1 | 1 | 2 | 3 | — | — | 发闾左適戍渔阳九百人，屯大泽乡。 | 《史记·陈涉世家》 |
| qin_003 | 秦锐士 | 普通 | 兵牌 | 步兵 | 中 | 3 | 1 | 1 | 1 | 3 | 5 | — | — | 魏氏之武卒，不可以遇秦之锐士。 | 《荀子·议兵》 |
| qin_004 | 劲弩手 | 普通 | 兵牌 | 弓兵 | 轻 | 2 | 1 | 1 | 2 | 3 | 2 | — | — | 秦带甲百余万，车千乘，骑万匹。 | 《史记·张仪列传》 |
| qin_005 | 连弩台 | 普通 | 兵牌 | 支援 | 重 | 3 | 2 | 1 | 2 | 3 | 4 | — | 部署当回合，指定一个友方单位本回合HP+3 | 备高临以连弩之车；鼓之舞之，三军奋击。 | 《墨子·备高临》(化用) |
| qin_006 | 疾驰轻骑 | 普通 | 兵牌 | 骑兵 | 轻 | 2 | 1 | 2 | 1 | 2 | 3 | 闪击 | — | 车骑之精，轻驰如风，掠野奔袭。 | 《孙膑兵法·八阵》(化用) |
| qin_007 | 弩矢督 | 普通 | 策略牌 | — | — | 1 | 0 | — | — | — | — | — | 对敌方随机单位造成3点伤害 | 矢石之积，皆入于仓，以时给之。 | 睡虎地秦墓竹简·仓律(化用) |
| qin_008 | 攒射 | 普通 | 策略牌 | — | — | 2 | 0 | — | — | — | — | — | 对一名敌方兵牌造成3点伤害 | 劲弩攒发，矢如飞蝗。 | 《墨子·备城门》(化用) |
| qin_009 | 斥候骑 | 稀有 | 兵牌 | 骑兵 | 轻 | 3 | 1 | 2 | 1 | 3 | 4 | 闪击、伏兵 | — | 斥候远窥，昼伏夜驰，以探敌情。 | 《武经总要》(化用) |
| qin_010 | 轻车 | 稀有 | 兵牌 | 骑兵 | 中 | 3 | 2 | 2 | 1 | 3 | 4 | 闪击、连战 | — | 小戎俴收，五楘梁辀。 | 《诗经·秦风·小戎》 |
| qin_011 | 穿杨弩手 | 稀有 | 兵牌 | 弓兵 | 中 | 3 | 1 | 1 | 2 | 4 | 3 | — | — | 去柳叶百步而射之，百发百中。 | 《战国策·西周策》(化用) |
| qin_012 | 铁鹰剑士 | 稀有 | 兵牌 | 步兵 | 重 | 4 | 2 | 1 | 1 | 5 | 4 | 重甲1 | — | 王于兴师，修我甲兵，与子偕行。 | 《诗经·秦风·无衣》 |
| qin_013 | 陷阵士 | 稀有 | 兵牌 | 步兵 | 中 | 4 | 1 | 1 | 1 | 4 | 4 | 血战 | — | 选锐冲之，分兵继之，急击勿疑。 | 《吴子·料敌》 |
| qin_014 | 飞羽轻骑 | 稀有 | 兵牌 | 骑兵 | 轻 | 4 | 1 | 2 | 1 | 6 | 4 | 闪击 | — | 追亡逐北，伏尸百万，流血漂橹。 | 贾谊《过秦论》(化用) |
| qin_015 | 军功爵 | 稀有 | 策略牌 | — | — | 4 | 0 | — | — | — | — | — | 指定一个友方单位本回合ATK+2、HP+3；抽取1张牌 | 能得甲首一者，赏爵一级，益田一顷，益宅九亩。 | 《商君书·境内》 |
| qin_016 | 反间 | 稀有 | 策略牌 | — | — | 2 | 0 | — | — | — | — | — | 弃置敌方1张手牌 | 秦多与赵王宠臣郭开金，为反间。 | 《史记·廉颇蔺相如列传》 |
| qin_017 | 连弩士 | 史诗 | 兵牌 | 弓兵 | 轻 | 3 | 1 | 2 | 2 | 4 | 2 | 连战 | — | 自以连弩候大鱼出射之。 | 《史记·秦始皇本纪》 |
| qin_018 | 绝粮道 | 史诗 | 策略牌 | — | — | 2 | 0 | — | — | — | — | — | 抽取1张牌；对敌方随机单位造成3点伤害 | 又分其兵，绝其粮道，赵军乏食而乱。 | 《史记》(化用) |
| qin_019 | 锐士营 | 史诗 | 兵牌 | 步兵 | 重 | 5 | 2 | 1 | 1 | 5 | 5 | 守护、重甲1 | — | 秦性强，其地险，其政严，其赏罚信，其人不让，皆有斗心。 | 《吴子·料敌》 |
| qin_020 | 商君变法 | 史诗 | 策略牌 | — | — | 4 | 0 | — | — | — | — | — | 抽取1张牌；为一座己方建筑回复7点HP | 令既具，未布，恐民之不信，已乃立三丈之木于国都市南门。 | 《史记·商君列传》 |
| qin_021 | 移民实边 | 史诗 | 策略牌 | — | — | 6 | 0 | — | — | — | — | — | 抽取3张牌 | 明赏罚，劝耕战，民勇于公战，怯于私斗。 | 《史记·商君列传》(化用) |
| qin_022 | 玄甲铁骑 | 传说 | 兵牌 | 骑兵 | 重 | 6 | 2 | 2 | 1 | 6 | 6 | 闪击、血战 | — | 岂曰无衣？与子同裳。王于兴师，修我甲兵。 | 《诗经·秦风·无衣》 |
| qin_023 | 函谷铁卫 | 传说 | 兵牌 | 步兵 | 重 | 6 | 2 | 1 | 1 | 6 | 6 | 守护、重甲1 | — | 深沟高垒，分兵固守，使敌欲战不得。 | 《吴子·应变》(化用) |
| qin_024 | 蹶张神弩 | 传说 | 兵牌 | 弓兵 | 重 | 6 | 2 | 1 | 2 | 7 | 7 | — | — | 弩出于蹶张，射远命中，所当皆靡。 | 《武经总要》(化用) |

#### 汉朝卡池（Han，24 张）

| cardId | 名称 | 稀有度 | 类型 | 兵种 | 重量 | 部署费用 | 行动费用 | 行动次数 | 射程 | ATK | HP | 词条 | 效果 | 古文描述 | 出处 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| han_001 | 汉弩手 | 普通 | 兵牌 | 弓兵 | 轻 | 2 | 1 | 1 | 2 | 3 | 2 | — | — | 汉弩一石十二斤，射程六百步。 | 《汉书·匈奴传》(化用) |
| han_002 | 材官骑士 | 普通 | 兵牌 | 骑兵 | 轻 | 1 | 1 | 2 | 1 | 2 | 1 | 闪击 | — | 一岁为卫士，一岁为材官骑士。 | 《汉书·食货志》 |
| han_003 | 边郡戍卒 | 普通 | 兵牌 | 步兵 | 中 | 2 | 1 | 1 | 1 | 2 | 3 | — | — | 烽火通于甘泉、长安。 | 《汉书·匈奴传》 |
| han_004 | 材官 | 普通 | 兵牌 | 步兵 | 中 | 3 | 1 | 1 | 1 | 3 | 5 | — | — | 材官、骑士，各有员数。 | 《汉书·高帝纪》 |
| han_005 | 治粟都尉 | 普通 | 兵牌 | 支援 | 重 | 3 | 2 | 1 | 2 | 3 | 4 | — | 部署当回合，指定一个友方单位本回合HP+3 | 桑弘羊为治粟都尉，领大农，尽代仅斡天下盐铁。 | 《史记·平准书》(化用) |
| han_006 | 游徼骑 | 普通 | 兵牌 | 骑兵 | 中 | 3 | 2 | 2 | 1 | 3 | 4 | 闪击、连战 | — | 繇役轻骑，往来如风。 | 《汉书》(化用) |
| han_007 | 招降 | 普通 | 策略牌 | — | — | 1 | 0 | — | — | — | — | — | 对敌方随机单位造成3点伤害 | 招降纳叛，来者不拒。 | 《汉书》(化用) |
| han_008 | 屯田 | 普通 | 策略牌 | — | — | 3 | 0 | — | — | — | — | — | 为一座己方建筑回复5点HP；抽取1张牌 | 募民徙塞下，皆便田作。 | 《汉书·晁错传》(化用) |
| han_009 | 轻车骑 | 稀有 | 兵牌 | 骑兵 | 轻 | 3 | 1 | 2 | 1 | 3 | 4 | 闪击、伏兵 | — | 轻车锐骑，昼夜兼行。 | 《汉书·卫青传》(化用) |
| han_010 | 材官蹶张 | 稀有 | 兵牌 | 弓兵 | 中 | 3 | 1 | 1 | 2 | 4 | 3 | — | — | 材官蹶张，弓弩并发。 | 《汉书·申屠嘉传》(化用) |
| han_011 | 屯田卒 | 稀有 | 兵牌 | 步兵 | 中 | 3 | 1 | 1 | 1 | 4 | 2 | 守护 | — | 且耕且守，边备以充。 | 《汉书·赵充国传》(化用) |
| han_012 | 玄甲校士 | 稀有 | 兵牌 | 步兵 | 重 | 4 | 2 | 1 | 1 | 5 | 4 | 重甲1 | — | 玄甲曜日，望之如墨。 | 《汉书》(化用) |
| han_013 | 死士营 | 稀有 | 兵牌 | 步兵 | 中 | 4 | 1 | 1 | 1 | 4 | 4 | 血战 | — | 陷阵之志，有死无生。 | 《后汉书·耿弇传》(化用) |
| han_014 | 骁骑 | 稀有 | 兵牌 | 骑兵 | 轻 | 4 | 1 | 2 | 1 | 5 | 5 | 闪击 | — | 骁骑十万，旌旗蔽野。 | 《汉书·匈奴传》(化用) |
| han_015 | 破敌封赏 | 稀有 | 策略牌 | — | — | 4 | 0 | — | — | — | — | — | 指定一个友方单位本回合ATK+2、HP+3；抽取1张牌 | 斩首捕虏，赐爵有差。 | 《汉书·景帝纪》(化用) |
| han_016 | 离间 | 稀有 | 策略牌 | — | — | 2 | 0 | — | — | — | — | — | 抽取1张牌；对敌方随机单位造成3点伤害 | 陈平多阴谋，离间楚君臣。 | 《汉书·陈平传》(化用) |
| han_017 | 射声士 | 史诗 | 兵牌 | 弓兵 | 轻 | 3 | 1 | 2 | 2 | 4 | 2 | 连战 | — | 射声校尉，闻声而中。 | 《汉书·百官公卿表》(化用) |
| han_018 | 决水灌城 | 史诗 | 策略牌 | — | — | 4 | 0 | — | — | — | — | — | 指定一个友方单位本回合HP+2；对指定一排单位造成2点伤害 | 决荥阳之水以灌城，城坏。 | 《史记·高祖本纪》(化用) |
| han_019 | 虎贲营 | 史诗 | 兵牌 | 步兵 | 重 | 5 | 2 | 1 | 1 | 4 | 5 | 守护、重甲2 | — | 虎贲之士，百人有余。 | 《汉书·王莽传》(化用) |
| han_020 | 白马校尉 | 史诗 | 兵牌 | 骑兵 | 轻 | 5 | 1 | 2 | 1 | 5 | 6 | 闪击、伏兵 | — | 白马义从，所向披靡。 | 《后汉书·公孙瓒传》(化用) |
| han_021 | 盐铁论 | 史诗 | 策略牌 | — | — | 5 | 0 | — | — | — | — | — | 限制敌方一个单位一回合行动；为一座己方建筑回复5点HP；为一个友方单位回复3点HP | 笼天下盐铁，以佐赋税。 | 《盐铁论》(化用) |
| han_022 | 骠骑精骑 | 传说 | 兵牌 | 骑兵 | 重 | 6 | 2 | 2 | 1 | 6 | 6 | 闪击、血战 | — | 匈奴未灭，无以家为。 | 《汉书·霍去病传》(化用) |
| han_023 | 羽林壁垒 | 传说 | 兵牌 | 步兵 | 重 | 6 | 2 | 1 | 1 | 6 | 4 | 守护、重甲2 | — | 壁垒森严，胡骑不敢南牧。 | 《汉书》(化用) |
| han_024 | 汉家大黄弩 | 传说 | 兵牌 | 弓兵 | 重 | 6 | 2 | 1 | 2 | 7 | 6 | 召唤(牌堆) | — | 大黄参连弩，射及数百步。 | 《汉书·李广传》(化用) |
<!-- POOL:END -->

### 4.5 数值框架（费用曲线）

**单局 CP 曲线：**
- 第 1 回合：1 CP → 只能出 1~2 费卡
- 第 3 回合：3 CP → 可出 2+1 或单张 3 费
- 第 5 回合：5 CP → 进入中费核心节奏
- 第 7 回合+：7~12 CP → 高费终结或铺场

> 可使用策略卡增加CP上限，或者回复当前回合CP，或者增加下一回合临时CP上限

**费用分布建议（30 张卡组）：**

| 费用 | 张数 | 定位 |
|------|------|------|
| 1~2 费 | 10~12 张 | 前期铺场、站场 |
| 3~4 费 | 12~14 张 | 中坚核心、节奏点 |
| 5 费+ | 4~6 张 | 高费终结、翻盘卡 |

**两个朝代卡池的实际分布（各 24 张，与《数据表demo.xlsx·卡池总览》第二节一致）：**

| 朝代 | 1费 | 2费 | 3费 | 4费 | 5费 | 6费+ | 节奏特征 |
|------|-----|-----|-----|-----|-----|------|---------|
| 秦·赳赳老秦 | 2 | 6 | 6 | 5 | 1 | 4 | **两头重**：低费铺场换场面，6 费重锤一波定局 |
| 汉·大汉雄风 | 2 | 3 | 8 | 5 | 3 | 3 | **平原化**：3 费中坚占三分之一，每回合稳定落子、不靠爆费 |

> 两朝分布刻意不同，以满足「整体卡组节奏不同」的要求；单张卡的面板允许有相近之处。
> **例外（不可省）**：两朝各含 1 张**支援（器械）**（其 buff 效果计入卡牌价值）、各含 1 张 **1 费策略牌**（随机直伤 3 点，基础表 B61 = 1 费）。

---

### 4.6 与 Excel 工作流对应
Excel 设计表（设计阶段）
│
▼
手动创建 ScriptableObject（Demo 阶段）
│
▼
Card.asset → 拖入 Prefab → 运行时加载

**ScriptableObject 字段映射：**

```csharp
public enum Rarity { Standard, Limited, Special, Elite } // 普通/稀有/史诗/传说

[CreateAssetMenu(fileName = "NewCard", menuName = "千秋策/卡牌")]
public class CardData : ScriptableObject
{
    public string cardId;
    public string cardName;
    public int cost;
    public Rarity rarity;           
    public CardType cardType;
    public int atk;
    public int hp;
    public UnitType unitType;
    public WeightClass weight;
    public List<Keyword> keywords;
    public string effectText;
    public string flavorText;
}
```

## 五、卡组规则
### 5.1 基础规则
> 单一卡组内由 30 张卡牌构成

### 5.2 稀有规则

> 最终卡组构筑为 12普通 + 8稀有 + 4史诗 + 2传说 + 4任意普通/稀有/史诗卡 + 大营、军械库、粮草营固定卡

其中，普通卡同名卡限制 4 张，稀有卡同名限制 3 张，史诗卡同名限制 2 张，传说卡不可同名，由局内卡牌效果产生的卡牌不计入稀有度规则

### 5.3 朝代混编规则
#### 5.3.1 主要朝代
> 主要朝代的卡应最少不低于25张

#### 5.3.2 次要朝代
> 次要朝代的卡不多于 5 张，不得编入传说品质卡牌

### 5.4卡牌区说明
> 分为牌堆、手牌区、作战区 —— **本局不设弃置区**。

> 还未抽取或者由特殊操作放回的在牌堆，可以随机抽取

> 手牌区的当前回合可使用，消耗指挥点使用后兵牌进入作战区，策略卡结算后直接退场；手牌区上限 7 牌，超出上限的卡牌（最后加入的）直接退场

> 已经使用且未死亡的兵牌位于作战区，可消耗行动力进行操作

> **牌堆只减不增**：所有离场卡牌——已结算的策略卡、被效果弃置的手牌、死亡的单位卡、超出手牌上限的牌——一律**直接退场，不再回到牌堆**。
> 取消弃置区后不存在「弃牌堆洗回牌堆」的回收规则，因此**牌堆摸空是必然会发生的中后期事件**，其后果由 §5.4.1 的疲劳结算接管。

#### 5.4.1 牌库耗尽（疲劳）结算

> 取代旧版「牌堆清空后把弃置区的牌重新洗牌放回牌堆」的回收规则。牌堆摸空后**不回收、不洗牌**，
> 改为按 KARDS 口径，对**抽不到牌的那一方**结算伤害。

| 项 | 规则 |
|------|------|
| **触发时机** | 任何一次**实际尝试抽牌**（抽牌阶段抽 1 张、或抽牌类策略卡）而牌堆已无牌可抽的瞬间，**逐次**触发 |
| **结算对象** | 抽不到牌的**那名玩家自己的大营**（KARDS 口径：牌库炸的是自己） |
| **伤害数值** | 第 1 次 **2** 点，此后**每次 +2**：2 → 4 → 6 → 8 → 10 …… |
| **计数器** | 双方**各自独立**计数（各算各的牌堆），永不重置、只增不减 |
| **多抽叠加** | 一次抽多张时**逐张结算**：牌堆空时打「摸牌3」= 触发 3 次 = 2+4+6 = **12 点自伤** |
| **不可减免** | 该伤害**不是攻击**：不触发反击、不受「守护」转移、不吃重甲减免、与军械库 Debuff 无关，直扣大营 HP |
| **胜负联动** | 大营 HP ≤ 0 立即判负（§2.2 / §8.2），因此牌堆摸空后拖延 = 被疲劳直接终结 |
| **UI 表现** | 牌堆图标转空状态 + 飘字「牌库已空」；大营上方飘红色伤害数字；HUD 常驻显示「疲劳：每次扣 X 点」 |

**设计影响（取消弃置区的连带后果）：**

- **对局存在硬性上界**：30 张卡组（§5.1）开局摸 5 张后，正常每回合抽 1 张，约**第 25 回合前后**摸空。
  疲劳随即接管节奏，替「龟缩拖时间」的一方判死刑，避免双方对峙到无限回合。
- **抽牌类策略卡变成双刃剑**：「摸牌1/摸牌3」在牌堆未空时是加速找关键牌的手段；**牌堆已空时纯属自伤**
  （摸牌3 = 12 点），玩家必须在"过牌"和"炸自己"之间做判断。
- **「召唤(牌堆)」的边界**：汉·汉家大黄弩把新卡加进牌堆，是唯一能让牌堆**增加**的效果（§4.3），
  在空牌堆时等于**给己方续一次命**，是可预期的疲劳解药。


## 六、AI 设计
### 6.1 设计定位

Demo 阶段的 AI 为**基于规则的评分型 Bot**，不使用行为树或机器学习，确保：
- 代码量可控（1~2 个脚本搞定）
- 调试透明（每个决策可打印分数，方便查 bug）
- 运行轻量（手机端无压力）

| 项目 | 说明 |
|------|------|
| **AI 类型** | 规则驱动 + 评分系统（Scoring System） |
| **决策方式** | 枚举所有合法行动 → 逐项打分 → 执行最高分 |
| **反应速度** | 固定延迟 0.8s~1.2s，模拟"思考"，方便玩家观察 |
| **难度分级** | 简单 / 普通（Demo 先实现简单难度） |
| **架构位置** | `Scripts/Core/AI/AIBot.cs`，由 `GameManager` 在敌方回合调用 |

---

### 6.2 评分系统核心

AI 每回合按**三层决策**执行，每层独立评分：
Layer 1: 出牌阶段（Hand → Board）
└─ 对每张手牌 × 每个合法入排位置（行 × 贴靠成员 × 左/右）打分
Layer 2: 行动阶段（Unit Action）
└─ 对每个可动单位 × 每个合法移动/攻击 打分
Layer 3: 结束判断
└─ 若无可行高分行动，点击结束回合
plain

**评分维度与权重（简单难度）：**

| 评分项 | 权重 | 说明 |
|--------|------|------|
| **斩杀检测** | +9999 | 若此行动能直接摧毁敌方大营，必做 |
| **建筑斩杀** | +500 | 若能摧毁军械库/粮草，高优先级 |
| **单位交换效率** | +ATK_diff × 10 | 我方攻击后，敌方死而我方活 = 高分 |
| **铺场价值** | +(ATK+HP)/cost × 5 | 费用效率高的卡优先出 |
| **前军压制** | +20 | 单位进入前军（威胁大营） |
| **大营守护** | +30 | 中军保留单位反打进入前军的敌方单位（大营在中军，排位制无遮挡） |
| **随机抖动** | -5 ~ +5 | 简单难度加入随机，避免完全最优显得像挂 |
| **违规惩罚** | -999 | 选错入排行/贴靠成员、超费、AP 不足等非法操作（过滤用） |

> **普通难度差异**：去掉随机抖动，权重调高 20%，会计算"下回合斩杀线"（预判 1 步）。

---

### 6.3 状态机（AI 姿态）

Bot 每回合开始时根据场面计算 **AIState**，影响评分倾向：

```csharp
public enum AIState { Aggressive, Defensive, Balanced }
表格
状态	触发条件	评分倾向
Aggressive（抢血）	敌方大营 HP ≤ 10 或我方场面优势（总 ATK ≥ 敌方 1.5 倍）	前军压制权重 ×2，单位交换权重 ×0.5（不怕换）
Defensive（防守）	我方大营 HP ≤ 10 或敌方场面优势	大营守护权重 ×3，优先解掉前军敌方单位
Balanced（均衡）	默认状态	按基础权重打分
状态切换示例：
第 1~3 回合：通常 Balanced，铺场
第 4 回合发现敌方大营只剩 8 HP：切 Aggressive，骑兵直插
我方大营被拆到 5 HP：切 Defensive，回防中军
6.4 决策流程伪代码
csharp
public class AIBot : MonoBehaviour
{
    public AIState currentState;
    public float thinkDelay = 1.0f;

    public IEnumerator TakeTurn()
    {
        yield return new WaitForSeconds(thinkDelay);
        
        // 1. 更新姿态
        currentState = EvaluateState();
        
        // 2. 出牌阶段
        var bestPlay = FindBestCardPlay();
        if (bestPlay.score > 0)
        {
            yield return ExecuteCardPlay(bestPlay);
            yield return new WaitForSeconds(0.5f);
        }
        
        // 3. 行动阶段（遍历所有可动单位）
        while (true)
        {
            var bestAction = FindBestUnitAction();
            if (bestAction.score < 50) break; // 没有值得做的行动
            
            yield return ExecuteUnitAction(bestAction);
            yield return new WaitForSeconds(0.6f);
        }
        
        // 4. 结束回合
        GameManager.Instance.EndTurn();
    }

    int ScoreAction(GameAction action)
    {
        int score = 0;
        
        // 斩杀优先
        if (action.killsEnemyCamp) return 9999;
        
        // 摧毁建筑
        if (action.destroysBuilding) score += 500;
        
        // 单位交换
        if (action.isAttack)
        {
            var target = action.targetUnit;
            var attacker = action.sourceUnit;
            
            // 我能打死他，他打不死我 = 大赚
            if (target.hp <= attacker.atk && attacker.hp > target.atk)
                score += 200;
            // 一换一 = 小赚（看费用差）
            else if (target.hp <= attacker.atk && attacker.hp <= target.atk)
                score += 50;
            // 换不掉 = 亏
            else
                score -= 100;
        }
        
        // 铺场效率
        if (action.isDeploy)
            score += (action.card.atk + action.card.hp) / action.card.cost * 5;
        
        // 前军压制
        if (action.movesToFrontRow)
            score += 20 * (currentState == AIState.Aggressive ? 2 : 1);
        
        // 随机抖动（简单难度）
        score += Random.Range(-5, 6);
        
        return score;
    }
}
```

### 6.5 特殊场景处理

|场景	|AI 行为|
|------|-------|
|手牌满 7 张且无法出牌|优先打出费用最低的策略卡清手牌，或结束回合
|CP 用不完但无合法入排位置|尝试移动单位攻击，或结束回合|
|敌方大营暴露（前军无单位）|无论状态，强制切 Aggressive，有闪击单位直接冲|
|袭扰骑兵 HP ≤ 2 或后军建筑已拆完|优先撤回己方前军保存战力（撤回后按普通骑兵评分）|
|我方建筑被拆|下回合优先出中费单位站场，不再贪前军|
|卡库抽空|进入疲劳倒计时（§5.4.1）：优先用场上单位＋手牌在疲劳滚起来前斩杀；牌堆已空时**不再打出抽牌类策略卡**（白付 CP 且加速自伤）；不再留牌过牌，改打最贵的场面牌|

### 6.6 难度分级

|维度|	简单 Bot|	普通 Bot（预留）|
|------|------|------|
|评分精度|	当前回合最优|	预判 1 回合（敌方下回合能斩杀我吗？）|
|随机抖动|	±5 分	|0|
|建筑保护|	不主动修建筑	|手牌有修缮卡时，HP < 3 的建筑会修|
|连战/闪击利用|	有闪击就下，但不计算 combo	|会算"闪击+移动+攻击"的连携|
|失误率|	10% 概率选第二优行动|	0%|

### 6.7 与代码架构的对接
```C
GameManager (回合总控)
    └─ 若 currentPlayer == 1（敌方）→ 调用 AIBot.TakeTurn()
        └─ AIBot 通过 Board / PlayerState 读取场面
            └─ 调用 GameManager.TryPlayCard() / Board.MoveUnit() 等接口执行
                └─ 操作完成后，UI 层自动响应（事件驱动）
```

AI 不直接操作 UI，所有行动通过 GameManager 的公共 API 完成，与玩家点击走的是同一条逻辑路径，避免"AI 开挂"或"UI 不同步"。

## 七、战斗细则
### 7.1 攻击交互（Attack Interaction）

#### 7.1.1 攻击发起条件

| 条件 | 规则 |
|------|------|
| **AP 检查** | 攻击者必须有剩余 AP（≥1） |
| **方向检查** | 只能攻击"前方"的排（己方后军 → 己方中军 → 共享前军 → 敌方中军 → 敌方后军） |
| **射程检查** | 目标排与攻击者所在排的排距 ≤ 兵种射程（步/骑 1 排，弓/器 2 排） |
| **目标合法性** | 目标排内存在敌方单位或建筑；不能攻击友方；被「守护」覆盖的目标不可选 |

**所有单位只能向前攻击**，不能攻击同排或身后的排。共享前军对双方均为"前方相邻排"，因此守方在中军、攻方在前军时互为对方的前方，**反击规则不受方向限制影响**。

#### 7.1.2 伤害结算流程（精确顺序）

```csharp
// 步骤 1：确定基础伤害
int baseDamage = attacker.atk;

// 步骤 2：应用词条修正（攻击方）

// 步骤 3：应用词条修正（受击方）

// 步骤 4：方向与射程校验（目标必须在攻击方前方，且排距 ≤ 兵种射程）
if (!IsInForwardRange(attacker, target)) return;   // 非法攻击，直接返回

// 步骤 5：扣减 HP
target.hp -= finalDamage;

// 步骤 6：死亡判定
if (target.hp <= 0)
    target.Die();  // 进入死亡流程，移出战场，直接退场（本局不再回到牌堆，见 §5.4）

// 步骤 7：反击判定（同类互攻才反击）
// 近战↔近战、远程↔远程：互相反击；近战↔远程交叉攻击：不反击
if (attacker.isMelee == target.isMelee && target.hp > 0)
{
    // 目标反击
    int counterDamage = target.atk;
    attacker.hp -= counterDamage;
    if (attacker.hp <= 0) attacker.Die();
}
```

#### 7.1.3 攻击后状态更新
攻击者：ap -= 1,ap = 0 时不可操作
若目标死亡：目标从战场移除，占用解除

#### 7.1.4 死亡处理
| 单位类型 | 来源 | 死亡后处理 |
|----------|------|-----------|
| 普通单位 | 卡组中的卡牌打出 | 移出战场，直接退场（本局不再回到牌堆，不进入任何区域） |
| 衍生单位 | 由卡牌效果召唤（如“召唤一个XX”），携带“召唤”标记 | 直接消失，同样不再回到牌堆 |

### 7.2 策略卡结算
#### 7.2.1 通用结算流程
```
玩家打出策略卡
  ├─ 1. 费用检查：当前 CP ≥ 卡面 cost？
  ├─ 2. 目标选择：玩家指定合法目标排/单位（按卡面要求）
  ├─ 3. 效果结算：立即执行卡面描述的效果
  ├─ 4. 卡牌退场：该策略卡结算后直接退场（本局不再可用，见 §5.4）
  └─ 5. 状态检查：是否有单位死亡/建筑被毁 → 触发判定
```
### 7.2.2 可选择目标类型
|策略卡类型|合法目标|非法目标|
|------|-----|-----|
|伤害类|敌方单位/建筑，或敌方排（按卡面）|友方单位或己方排|
|buff类|友方单位，或己方排（按卡面）|敌方单位或敌方排|
|debuff类|敌方单位，或敌方排（按卡面）|友方单位或己方排|
|抽卡过牌类|无目标（空放）||
|弃置敌方牌类|敌方手牌区（被弃的牌直接退场，本局不再回到牌堆）||
|维修类|己方建筑|非建筑单位|

> 目标合法性还受「守护」限制：被守护覆盖的**同排相邻单位/建筑**不能被选中（攻击与策略卡均不可指定），详见 §4.3；以**排**为目标的范围效果不选中具体单位，**不受守护影响**。

#### 7.2.3 反制牌（埋伏型，未定价）

反制是一类**独立的策略牌子类**，与 7.2.1 的主动结算流程不同：

| 项 | 规则 |
|------|------|
| **使用方式** | **不能主动打出**，手牌中的反制牌在整个己方回合并不可选（卡面呈暗色，点击提示"此牌需触发发动"） |
| **发动时机** | 满足其**触发条件**时自动发动；**不消耗费用**（不占当回合 CP） |
| **发动后** | 结算触发效果，该反制牌从手牌区**直接退场**（与其他策略牌一致，见 §5.4） |
| **设计意图** | 对方在看不到反制的情况下进攻，需要承担"踩到埋伏"的风险，从而**为防守方争夺节奏**、起出奇制胜的解场作用 |
| **与守护的区别** | 守护是**明面**的选中限制；反制是**暗牌**，对方无法预知，只有在触发条件成立的瞬间才暴露 |

> **已确认的示例（待落表）**：触发条件 = 敌方单位攻击己方大营；触发效果 = **该攻击者攻击后立即死亡**。
> 即"打大营的第一个单位有去无回"，用于惩罚对手把先手单位直接怼在大营上。
>
> **落表前仍需补两项**（《基准表外条目》已登记）：
> ① **触发条件的枚举**——例如：敌方攻击己方大营 / 敌方部署 ≥N 费单位 / 己方单位被消灭时；
> ② **触发效果的基准锚点**——本例的"攻击后立即死亡"接近 B63「消灭一个敌方随机单位」= 6 费，
> 但因**条件触发、不可主动选择时机**，其等价费用应低于 6；具体档位由策划确定。
>
> 在补齐上述两项之前，反制**不进入任何朝代卡池**，两个卡池的 48 张卡里没有反制牌。

### 7.3 维修卡说明
军械库或粮草营被摧毁后，可使用维修卡进行维修，然后获得一回合无敌。
具体效果：
该建筑获得 1 回合无敌（显示金色护盾图标）
无敌期间：免疫所有伤害，不可成为攻击/策略目标
无敌回合数：仅持续 1 个敌方回合 + 1 个己方回合，下一轮己方回合时失效
无敌结束时：建筑恢复正常状态，可再次被攻击
UI 表现：
建筑所在排的图标上方显示金色护盾图标
悬停建筑时 Tooltip 显示 "无敌（剩余 X 回合）"
设计意图：防止玩家在同一回合内反复用低费修缮卡刷建筑，强制对方必须先处理其他威胁。

### 7.4 骑兵特殊机制（袭扰）

**骑兵是唯一可以进入敌方中军的单位**，用于限制敌方部署并攻击敌方后军建筑。

- **进入条件**：己方骑兵位于共享前军时，消耗 1 AP 移动进入敌方中军；要求敌方中军仍有**空余单位容量**（即除大营外，中军内单位成员 < 4）。进入后该骑兵按 §2.3 入排规则贴入敌方中军链。
- **袭扰状态**（进入敌方中军后立即生效）：
```
1.只能攻击敌方后军建筑（军械库/粮草），不能攻击任何单位；
2.被攻击时不反击（守方单位可无损集火它）；
3.从进入后的下一个回合开始，每回合开始时 ATK +1、受到 2 点伤害（倒计时自毁，代表孤军断粮）。
```
- **占位效果**：袭扰骑兵占去敌方中军 **1 个单位容量**，使敌方中军留给对方单位可用的容量由 4 降为 3，进而触发 §8.1.1 的后军部署规则。
- **反制方式**：敌方中军单位与大营都**不能**攻击同排的袭扰骑兵（只能向前攻击）；守方只能用己方**后军**单位反打（后军向前 1 排即覆盖己方中军；后军的弓兵/器械射程 2，可同时覆盖中军与前军）。
- **与建筑分布的关系**：大营位于中军，因此袭扰骑兵**不能直接攻击大营**，只能拆后军的军械库/粮草（与 §2.3 一致）。
- **被守护的建筑**：若己方后军的守护单位与军械库/粮草**链相邻**（如后军链 `[军械库][盾·守护][粮草]`，盾同时是二者直接邻居），则二者不可被选中、袭扰骑兵**无合法攻击目标**（此时敌方只能先用弓兵/器械从共享前军击杀该守护单位，射程 2 可覆盖后军；因敌方无法往己方后军贴单位，通常无法靠切断相邻解除保护）。
- **撤回**：袭扰骑兵是**唯一可以向后移动**的单位，可消耗 1 AP 从敌方中军撤回**共享前军**。
  - 离开敌方中军后，**袭扰状态与袭扰累计的 ATK 加成一并移除**：ATK 回到卡面基础值、解除"只能攻击建筑"的限制、停止每回合 -2 HP 的自毁结算。
  - 撤回后若还有剩余 AP，可按普通骑兵规则继续行动（如撤回 + 攻击敌方中军）。
  - 再次进入敌方中军时袭扰状态**从零重新累计**（ATK 从卡面基础值重新开始每回合 +1）。


## 八、完整游戏流程
### 8.1 单局游戏流程
主菜单——>预选已构筑卡组/构筑新卡组——>进入对战——>进行对局——>胜负判定——>播放结算动画——>弹出结算界面（段位或者天梯分）——>返回主菜单/再来一局

#### 8.1.1 单位放置
场景：己方中军 4 个单位容量已满（其中可能有敌方袭扰骑兵占去 1 个），玩家手牌有单位卡想打出。
处理：
```
默认：单位卡只能部署进己方中军（单位容量未满时），入排时绕该排既有成员（含大营）左右贴放入链；
例外：中军单位容量已满、且中军内驻有敌方袭扰骑兵时，允许部署进己方后军（用于反打袭扰骑兵，见 §7.4）；
其余情况：所有单位卡变灰（CanvasGroup.alpha = 0.5），点击单位卡时飘字"中军已满，无法部署"；
玩家只能：① 使用策略卡 ② 移动现有单位腾出容量 ③ 结束回合
```
> 后军容量 3 = 军械库 + 粮草 + 至多 1 个单位；当后军那 1 个单位容量已被占用时，同样无法部署。

### 8.2 胜负结算流程

| 触发条件 | 流程 |
|---------|------|
| **敌方大营 HP ≤ 0** | 1. 暂停所有操作<br> 2. 播放胜利/失败动画（0.5s） <br>3. 弹出结算面板 |
| **己方大营 HP ≤ 0** | 同上 |

#### 8.2.1 结算面板

| 元素 | 说明 |
|------|------|
| 胜负标识 | "胜" / "负" 大字 + 朝代主题色背景 |
| 回合数 | 本局进行了 N 回合 |
| 关键数据 | 己方造成伤害 / 摧毁建筑数 / 使用策略卡数 |
| 操作按钮 | 【再来一局】 【返回主菜单】 |
| 分数结算 | 根据局内造成的伤害和回合数消耗等计算|

> **Demo 阶段**：不做复杂数据统计，只显示胜负 + 回合数 + 两个按钮。

#### 8.2.2 再来一局

| 项目 | 实现方式 |
|------|----------|
| 操作 | 点击【再来一局】按钮 |
| 效果 | 重新加载Battle.unity场景，不返回主菜单 |
| 对手 | 与上一局相同的AI对手 |
| 卡组 | 复用上一局选择的卡组 |
| 过程 | 加载场景时显示简短加载画面（Loading图标或黑屏过渡） |

### 8.3 战绩存储（Demo 简化）

```csharp
// 使用 PlayerPrefs 存本地战绩
public static class GameRecord
{
    public static int WinCount 
    { 
        get => PlayerPrefs.GetInt("MS_Win", 0); 
        set => PlayerPrefs.SetInt("MS_Win", value); 
    }
    public static int LoseCount 
    { 
        get => PlayerPrefs.GetInt("MS_Lose", 0); 
        set => PlayerPrefs.SetInt("MS_Lose", value); 
    }
}
```

主菜单显示战绩/分数，可供查看战绩和全局回放


## 九、主菜单和界面系统
### 9.1 菜单
```
┌─────────────────────────────┐
│                             │
│        千 秋 策              │
│     Millennia Stratagem     │
│                             │
│        [ 开始游戏 ]          |
|        [ 卡组收集 ]          |
|        [ 卡组构筑 ]          |
│        [ 规则说明 ]          │
│         [ 设  置 ]          │
│         [ 退  出 ]          │
│                             │
│          战绩               │
│                             │
└─────────────────────────────┘
```

### 9.2 各按钮功能
|按钮|功能|
|------|-----|
|开始游戏|开始一局游戏|
|卡组收集|抽取新的卡牌并查看现有卡牌|
|卡组构筑|构筑一套新的卡组或者调整现有卡组|
|规则说明|对游戏内的各种知识进行说明|
|设置|调整游戏声音，分辨率等|
|退出|退出游戏|
|战绩|查看战绩，提供全局回放功能|

## 十、UI交互
### 10.1 攻击交互流程

```
玩家长按己方单位（有 AP 且未行动）
    ↓
该单位所在排高亮（绿色边框） → 可移动的前方相邻排高亮（蓝色半透明覆盖） → 玩家拖向目标排移动
    ↓                                                               ↓
可攻击的敌方排高亮（红色半透明覆盖）                               播放移动动画
    ↓
玩家拖向目标排内的敌方单位/建筑攻击
    ↓
播放攻击动画（DOTween 位移 0.2s → 震屏 0.1s → 伤害数字弹出）
    ↓
双方 HP 更新，死亡单位淡出移除
```

> 移动只能向前（唯一例外：袭扰骑兵撤回，见 §7.4）；袭扰骑兵额外显示"撤回"箭头（指向己方前军），拖拽即消耗 1 AP 撤回，撤回后清除袭扰标记与 ATK 加成。

### 10.2 卡牌查看
|操作|	反馈|
|----|----|
|悬停（Hover）	|手牌放大 1.15 倍，Y 轴上移 30px，显示详细 Tooltip|
|点击选中	|卡牌边框变金色，合法部署行与可贴靠成员高亮（默认己方中军；满足 §8.1.1 条件时加显后军）|
|点击空白处取消|	卡牌归位，高亮消失|

### 10.3 非法操作提醒
|非法操作	|UI 反馈|
|------|--------|
|CP 不足	|卡牌变灰（CanvasGroup.alpha = 0.5），点击时抖动 0.1s，飘字"指挥点不足"|
|AP 不足	|单位点击无反应，或飘字"行动力已耗尽"|
|非法部署排	|点击不可部署的排时，该排红闪 0.2s，飘字"只能部署到中军"|
|手牌上限	|抽牌时手牌已有 7 张，新抽的牌**直接退场**（本局不再可用）并提示"手牌已满"|
|射程不足	|点击目标时，目标排红闪，飘字"目标超出射程"|
|被守护目标	|点击受守护保护的单位/建筑时红闪，飘字"该目标受守护保护"|
|中军已满	|点击单位卡时飘字"中军已满，无法部署"；中军驻有敌方骑兵时改提示"可部署到后军"|


## 十一、后续规划
### 11.1版本预计
|版本|功能|
|-----|-----|
|v0.1|demo版本，可支持单机本地对战，具备基础功能|
|v0.2|demo扩展，预计引入更强的AI并增加更多可选卡包|
|v0.3|demo扩展，引入更多的卡牌|
|v1.0|上线steam，支持联网对战，设置排位等|
|v1.0+|逐渐扩展卡包并对数值机制进行微调，平衡游戏体验|
|v2.0|上线mobile端，支持双端互通|
|v2.0+|继续平衡性调整|

### 11.2 技术债务预留
网络同步：GameManager 所有状态变更通过 CmdXXX / RpcXXX 方法包装，后续接 Mirror 时只需改方法内部
数据持久化：PlayerState 已实现 Serialize() / Deserialize()，后续存云端直接复用
多语言：所有文本走 LocalizationManager.Get("key")，Demo 阶段只填中文，英文 key 留空